# Avalonia MVVMExpress for Visual Studio

Tools → **Avalonia MVVMExpress** → Create New App… / Add Page…

Installs `Plugin.Avalonia.MVVMExpress.Templates` and runs `dotnet new`. Marketplace upload is manual from the `ide-extensions` workflow artifact.
